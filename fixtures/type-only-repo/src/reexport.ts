// Re-exports name nothing locally, so what survives is decided by the meaning of
// the re-exported symbol in the module it came from - `Contract` is an interface.
export { Base } from "./values";
export { Contract } from "./values";
